/**
 * Rive Web (Canvas) runtime — single import surface for this package.
 */
export {
  Rive,
  RiveFile,
  Layout,
  Fit,
  Alignment,
  StateMachineInput,
  EventType,
  ViewModelInstance,
  RuntimeLoader,
  LoopType,
} from '@rive-app/canvas';
export type {
  Event as RiveEvent,
  LayoutParameters,
  RiveParameters,
  RiveFileParameters,
  LoopEvent,
} from '@rive-app/canvas';
