/**
 * Rive Web (WebGL2) runtime — single import surface for this package.
 */
export {
  Rive,
  RiveFile,
  Layout,
  Fit,
  Alignment,
  StateMachineInput,
  EventType,
  ViewModelInstance,
  RuntimeLoader,
  LoopType,
} from '@rive-app/webgl2';
export type {
  Event as RiveEvent,
  LayoutParameters,
  RiveParameters,
  RiveFileParameters,
  LoopEvent,
} from '@rive-app/webgl2';
